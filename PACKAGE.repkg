// This is what it might look like

project repkg in "repkg" {
    build: cargo build --release

    test: cargo test

    postInstall {

    }

    dependencies {
        can-fail repkg install git
    }
}

project repkg-build in "repkg-build" {
    build: cargo build --release

    prePackage {
        //
    }

    postPackage {

    }
}

build: cargo build --release

test: cargo nextest run