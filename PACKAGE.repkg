project repkg in "repkg" {
    build {
        cargo build
    }

    test {
        cargo nextest run
    }
}
        
project repkg-build in "repkg-build" {
    build {
        cargo build
    }
    
    test {
        cargo test
    }
}
        
build : cargo build --release

test {
    cargo nextest run
}